{
  "name": "sifttt-source-pg",
  "version": "0.2.0",
  "description": "A sifttt source for Postgres.",
  "scripts": {
    "test": "docker-compose up --build --abort-on-container-exit"
  },
  "license": "MIT",
  "repository": "markbirbeck/sifttt-source-pg",
  "engines": {
    "node": ">=6 <7"
  },
  "dependencies": {
    "highland": "^2.10.2",
    "pg": "^7.3.0",
    "pg-query-stream": "^1.1.1"
  },
  "peerDependencies": {
    "sifttt": ">= 0.81.0"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^3.0.1"
  }
}
